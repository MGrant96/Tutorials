"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DateUtil = void 0;
var DateUtil = /** @class */ (function () {
    function DateUtil() {
    }
    DateUtil.getFromToDate = function (date) {
        if (date === void 0) { date = ''; }
        var dates = date.split(',');
        if (dates.length > 1) {
            var dateFrom = new Date(dates[0]);
            var dateTo = new Date(dates[1]);
            dateFrom.setUTCHours(0, 0, 0, 0);
            dateTo.setUTCHours(24, 59, 59, 59);
            if (dateTo.getTime() < dateFrom.getTime()) {
                var t = dateFrom.getTime();
                dateFrom = dateTo;
                dateTo = new Date(t);
            }
            return {
                from: dateFrom.getTime(),
                to: dateTo.getTime()
            };
        }
        else {
            var dateFrom = new Date(dates[0]);
            var dateTo = new Date(dates[0]);
            dateFrom.setUTCHours(0, 0, 0, 0);
            dateTo.setDate(dateFrom.getDate() + 1);
            dateTo.setUTCHours(0, 0, 0, 0);
            return {
                from: dateFrom.getTime(),
                to: dateTo.getTime()
            };
        }
    };
    DateUtil.timestampFromTo = function (strFrom, strTo) {
        return {
            from: new Date(strFrom).setHours(0, 0, 0, 0),
            to: new Date(strTo).setHours(23, 59, 59, 0)
        };
    };
    return DateUtil;
}());
exports.DateUtil = DateUtil;
//# sourceMappingURL=date.util.js.map